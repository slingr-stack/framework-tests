# Deploy-script configuration — written by ops/scripts/ after first run.
# Committed to git (non-secret identifiers only). Secrets are never written here.
APP_NAME=project-mgmt
RESOURCE_PREFIX=project-mgmt
REGION=us-central1
AR_REPO=drumr-apps
DB_INSTANCE_NAME=project-mgmt-db
DB_NAME=project_management_app
DB_USER=postgres
BACKEND_MAX_INSTANCES=1
FRONTEND_HOSTING=simple
STORAGE_TYPE=gcs
GCS_BUCKET=project-mgmt-app-files
BRANCH=develop
REPO_NAME=framework
PROJECT_ID=future-name-492914-n5
REPO_OWNER=
DOMAIN=tasky-app.dev
SEED_USER_EMAIL=sys@app.com
