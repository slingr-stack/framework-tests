---
name: frontend-tech-stack-conformance
description: >
  SR-* conformance agent for the frontend-tech-stack skill.
  Runs, updates, and interprets frontend-tech-stack.skill-conformance.spec.ts.
  Delegates to skill-conformance-generator for full scaffold regeneration.
tools:
  - read_file
  - file_search
  - grep_search
  - replace_string_in_file
  - run_in_terminal
---

# frontend-tech-stack Conformance Agent

**Skill:** `core/skills/frontend-tech-stack/SKILL.md`
**Spec:** `apps/project-management-app/backend/tests/unit/skill-conformance/frontend-tech-stack.skill-conformance.spec.ts`
**Score:** 93.3 (supporting, threshold 75 ✅)
**Tests:** 20 passing, 0 todos

## Run command

```bash
cd apps/project-management-app/backend
TS_NODE_PROJECT=tsconfig.test.json npx jest \
  --config config/jest.config.ts \
  --testPathPatterns='frontend-tech-stack.skill-conformance' \
  --no-coverage --verbose
```

## Fixtures

| Fixture | Role |
|---|---|
| `frontend/src/views/custom/SummaryView.tsx` | `DataTable` from `@drumr/framework-frontend` (not raw antd `Table`/`ProTable`); antd used only for primitive `Alert`/`Button`/`Card`/`Col`/`Row`/`Space`/`Typography` |
| `frontend/src/views/dataModels/tasks/helpers/taskFormLayout.tsx` | `DataField`+`DataForm` from `@drumr/framework-frontend`; `<DataField>` JSX not `<Input>`; antd imports are only layout primitives |
| `frontend/src/services/ActivityLogDataService.ts` | `GraphQLClient` from `@drumr/framework-frontend` injected via constructor; `dataAction()` GQL operation builder not `fetch`/`axios`; typed `@gql` result types |
| `frontend/src/views/dataModels/tasks/PendingTasksView.tsx` | `DataTable` from framework; antd used only for primitive `Card` |

## SR-* contract summary

| SR-* | Rules enforced |
|---|---|
| SR-1 | Framework components (`DataTable`/`DataField`/`DataForm`) used instead of raw antd `Table`/`ProTable`/`ProForm`; antd imports only primitive layout types (`Alert`, `Button`, `Card`, `Col`, `Row`, `Space`, `Typography`) |
| SR-2 | No `createRoot` in any app source file; no `ApolloProvider` import in any app source file; no `ReactDOM.render`/`ReactDOM.createRoot` |
| SR-3 | `GraphQLClient` from framework injected via constructor; `dataAction()` operation builder not raw `fetch`; typed `@gql` result types |
| SR-4 | Adversarial scan of all `frontend/src` (excluding `.umi*` build artefacts) — no `createRoot`; no `ApolloProvider`; no `ProTable` from pro-components; no `ProForm` from pro-components; no `axios` imports |

## Known issue: UMI build artefacts

`.umi/` and `.umi-production/` are auto-generated by UMI framework during build. They contain `axios` imports in generated plugin files (`plugin-request/request.ts`). The `collectSourceFiles()` walker excludes these directories via:
```typescript
const EXCLUDED_DIRS = new Set(['.umi', '.umi-production', 'node_modules']);
```
Do NOT remove this exclusion — it is intentional and correct.

## Raise score checklist

- Add `useQuery`/`useMutation` Apollo hook usage fixture → covers React hook-based data fetching pattern (currently only constructor injection pattern tested)
- Add Pro component `ProCard`/`ProDescriptions` second-choice usage fixture → covers the "Pro > antd" tier of the hierarchy
- Either closes remaining SR-3/SR-4 behavioral coverage gap → R 2→3 → 100
